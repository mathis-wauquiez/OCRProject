
# ── Python ──
*.pyc
__pycache__/

# ── Data (large binary files) ──
/data
*.pkl
*.gz
*.npz
*.json
*.parquet
*.fth
*.mlmodel

# ── Transient pipeline outputs (not committed) ──
/outputs

# ── Publishable results (committed so tables can be filled) ──
# Override global *.parquet / *.json / *.npz ignores for this folder:
!results/
!results/**
!results/**/*.parquet
!results/**/*.json
!results/**/*.npz

# ── Legacy output directories (kept for safety, can be removed) ──
/tests
/tests_2
/outputs_svg
/code_tests
/preprocessing_outputs
/saved figures
/saved_figures
/report_figures
/data/extracted
/clustering_evaluation

# ── Hydra run metadata ──
/.hydra_outputs
/hydra_outputs

# ── LaTeX build artifacts ──
*.aux
*.log
*.out
*.pdf
*.synctex.gz
*.fls
*.fdb_latexmk
*.bbl
*.blg

# ── Misc ──
OCR_Pipeline_Results.pptx
caches/hog_cache/params.json
/notebooks/codesave
