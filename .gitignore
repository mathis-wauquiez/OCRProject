
# ── Python ──
*.pyc
__pycache__/

# ── Data (large binary files) ──
/data
*.pkl
*.gz
*.npz
*.json
*.parquet
*.fth
*.mlmodel

# ── All pipeline outputs (extraction, preprocessing, clustering, etc.) ──
/outputs

# ── Legacy output directories (kept for safety, can be removed) ──
/tests
/tests_2
/outputs_svg
/code_tests
/preprocessing_outputs
/saved figures
/saved_figures
/report_figures
/data/extracted
/clustering_evaluation

# ── Hydra run metadata ──
/.hydra_outputs
/hydra_outputs

# ── Misc ──
OCR_Pipeline_Results.pptx
caches/hog_cache/params.json
/codesave
/notebooks/codesave
